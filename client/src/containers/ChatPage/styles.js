import styled from 'styled-components'

export const ChatWrapper = styled.div`
  display: flex;
  height: calc(100vh - 80px);

  .room {
    flex: 1;
    padding: 16px 32px;
    position: relative;
  }
`